server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flexpay
    username: postgres
    password: postgres
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/flexpay
    username: postgres
    password: postgres
  flyway:
    enabled: true
    baseline-on-migrate: true
  jackson:
    serialization:
      indent_output: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
